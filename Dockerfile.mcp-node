ARG BASE_TAG=main
FROM ghcr.io/obot-platform/mcp-images-base-node:${BASE_TAG}

ARG MCP_PACKAGE
ARG MCP_VERSION

USER root

# Install the MCP package globally
RUN npm install -g "${MCP_PACKAGE}@${MCP_VERSION}"

USER 1000
